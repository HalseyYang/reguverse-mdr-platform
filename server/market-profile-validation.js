import { profileFor } from '../src/features/device-profile/market-profile-configurations.js';
const p=v=>v!==undefined&&v!==null&&`${v}`.trim()!=='';
export function validateMarketProfile(region,profile={}){const c=profileFor(region,profile),missing=c.fields.filter(x=>x.required&&!p(profile?.[x.section]?.[x.name])).map(x=>x.name);if(region==='FDA'&&p(profile?.market?.recommended_submission_pathway)&&p(profile?.market?.selected_submission_pathway)&&profile.market.recommended_submission_pathway!==profile.market.selected_submission_pathway&&!p(profile?.market?.submission_pathway_change_rationale))missing.push('submission_pathway_change_rationale');const allowed=new Set(c.fields.map(x=>x.name)),incompatible=[];for(const [section,values] of Object.entries(profile))if(values&&typeof values==='object')for(const [name,value] of Object.entries(values))if(p(value)&&!allowed.has(name)&&/^(eu_|nmpa_|united_states_|existing_fda_|hong_kong_|domestic_|overseas_|single_registration_number)/.test(name))incompatible.push(name);return missing.length||incompatible.length?{code:'market_profile_validation_failed',missing:[...new Set(missing)],incompatible:[...new Set(incompatible)]}:{code:null,missing:[],incompatible:[]};}
